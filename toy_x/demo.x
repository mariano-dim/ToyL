begin
    var s: int;
    s:= 1;

    while (s <5)
    begin
        print(s);
        s := s+1;
    end

    for e := 8 to 10 do
    begin
       var local : int;
       local := 1000;
       print(e);
    end
end