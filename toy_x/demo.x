begin
    var s: int;
    s:= 1;

    while (s <5)
    begin
        print(s);
        s := s+1;
    end

    for e := 8 to 10 do
    begin
       var local : int;
       local := 1000;
       print(e);
    end

    var t: int;
    t:= 1;

    while (t <20)
    begin
        print(t);
        t:= t+1;
    end
end