begin
    var s: int;
    s:= 1;
    while (s <5)
    begin
        print(s);
        s := s+1;
    end

    for e := 1 (to) 10 do
    begin
        for f := 1 (to) 10 do
        begin
            print(f);
        end
        print(e);
    end

    if (s > 4)
    begin
        var cadena : string;
        cadena := "hola mundo";
        print(cadena);
    end

end