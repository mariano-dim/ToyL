begin
    for e := 0 (to) 2 do
    begin
        print(->"Variable e:" ->" Valor:" ->e+e+e+e+1+2+3-5 ->" ");
    end

    var iCounter : int;
    iCounter := 1;

    print(->"VariableiCoun==");
    print(->iCounter);

    print(->"Imprimiendo una matriz");
    for x := 0 (to) 2 do
    begin
        for y := 0 (to) 2 do
        begin
            print(->"x: " ->x ->"   " ->"y: " ->y);
        end
    end

end